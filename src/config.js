// const env = process.env.REACT_APP_BACKEND?.trim();
// const injected = typeof window !== "undefined" && window.__CONFIG__?.BACKEND;
// const sameOrigin = ""; // 배포에서 Nginx 리버스프록시 등으로 동일 오리진이면 빈 문자열 사용

// export const BACKEND =
//   env || injected || sameOrigin || "";